# AgentTeams 运行环境示例配置

复制为 `hiclaw-manager.env`（或按 AgentTeams 安装器引导生成）后按需修改。

```bash
# ---------- LLM ----------
# 使用已有 API Key 的模型服务，例如 qwen3.7-plus
LLM_PROVIDER=openai-compatible
LLM_BASE_URL=https://your-llm-endpoint.example.com/v1
LLM_API_KEY=your-api-key
LLM_MODEL=qwen3.7-plus

# ---------- Embedding（可选，用于 RAG/记忆检索）----------
EMBEDDING_ENABLED=true
EMBEDDING_MODEL=text-embedding-v3

# ---------- AgentTeams ----------
AGENTTEAMS_VERSION=v1.1.2
MANAGER_RUNTIME=qwenpow
WORKER_RUNTIME=qwenpow

# ---------- 服务端口 ----------
# Element Web 端口（默认 18088）
ELEMENT_WEB_PORT=18088
# CarSales mock 工具网关端口（tools/mock_tool_server.py）
MOCK_TOOL_GATEWAY_PORT=18089

# ---------- Matrix ----------
MATRIX_E2EE=false

# ---------- 工具网关（替换 create_agents_messages.md 中的占位符）----------
# 在单机 Docker 部署中，Worker 容器通过 Docker 网关 IP 访问宿主机 mock 网关：
# docker inspect -f '{{range .NetworkSettings.Networks}}{{println .Gateway}}{{end}}' hiclaw-manager
# 例如：MOCK_TOOL_BASE_URL=http://172.18.0.1:18089
MOCK_TOOL_BASE_URL=http://172.18.0.1:18089
```

## 说明

- 完整安装流程见 [AGENTTEAMS_RUNBOOK.md](AGENTTEAMS_RUNBOOK.md)。
- `MOCK_TOOL_BASE_URL` 必须在发送 `create_agents_messages.md` 前替换为实际可达地址。
- 本 demo 不依赖共享主机目录，Worker 通过 HTTP 访问 mock 工具网关。
